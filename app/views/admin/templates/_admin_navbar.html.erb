<nav class="navbar navbar-expand-lg bg-body-tertiary mb-3">
  <div class="container-fluid">
    <%= link_to t('baheth'), root_path, class: 'navbar-brand' %>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to t('.manage_speakers'), admin_speakers_path, class: "nav-link #{'active' if controller_name == 'speakers'}" %>
        </li>
        <li class="nav-item">
          <%= link_to t('.manage_playlists'), admin_playlists_path, class: "nav-link #{'active' if controller_name == 'playlists'}" %>
        </li>
        <li class="nav-item">
          <%= link_to t('.manage_media'), admin_media_path, class: "nav-link #{'active' if controller_name == 'media'}" %>
        </li>
        <li class="nav-item">
          <%= link_to t('.manage_cues'), admin_cues_path, class: "nav-link #{'active' if controller_name == 'cues'}" %>
        </li>
      </ul>

      <ul class="navbar-nav">
        <li class="nav-item d-flex">
          <%= link_to t('sign_out'), destroy_user_session_path, method: :delete, data: { turbo_method: :delete }, class: 'nav-link pe-0' %>
        </li>
      </ul>
    </div>
  </div>
</nav>
