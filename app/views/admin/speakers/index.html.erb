<h1>Speakers</h1>

<div id="speakers">
  <% @speakers.each do |speaker| %>
    <%= render speaker %>
    <p>
      <%= link_to "Show this speaker", admin_speaker_path(speaker) %>
    </p>
  <% end %>
</div>

<%= link_to "New speaker", new_admin_speaker_path %>
