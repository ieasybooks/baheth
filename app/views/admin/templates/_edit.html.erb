<div class="row justify-content-center">
  <div class="col-4">
    <div class="card">
      <div class="card-header">
        <span class="fs-4"><%= t(".#{controller_name.singularize}") %></span>
      </div>

      <div class="card-body">
        <%= render 'form' %>
      </div>
    </div>
  </div>
</div>
